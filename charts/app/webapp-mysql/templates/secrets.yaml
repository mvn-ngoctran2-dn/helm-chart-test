apiVersion: v1
kind: Secret
metadata:
  name: {{ include "webapp.fullname" . }}-secrets
type: Opaque
data:
  {{- range $key, $val := .Values.env.secret }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end }}
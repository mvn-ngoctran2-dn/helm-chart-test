apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysql-mvn.fullname" . }}-test-connection"
  labels:
    {{- include "mysql-mvn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysql-mvn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

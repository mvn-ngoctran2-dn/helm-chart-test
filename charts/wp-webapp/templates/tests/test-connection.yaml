apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wp-webapp.fullname" . }}-test-connection"
  labels:
    {{- include "wp-webapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wp-webapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "wp-webapp.fullname" . }}
  labels:
    {{- include "wp-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wp-webapp.selectorLabels" . | nindent 4 }}
